# Wait 60 seconds before timing out.
connect-timeout = 60

# Do not show failure document.
fail

# Automatically set the previous URL when follofing a location.
referer = ";auto"

# Use remote name for all URLs.
#remote-name-all

# Do not send a User-Agent header by default.
user-agent = ""
