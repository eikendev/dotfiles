#!/usr/bin/env zsh

set -o ERR_EXIT

basedir="$(dirname "$(realpath "${(%):-%x}")")"

TITLE='Thunderbird Unread'

source "$basedir/script-utils"

for cmd in inotifywait rg thunderbird-unread; do check_dependency $cmd; done

printf "%s\n" 'Launching Thunderbird Unread.'
exec thunderbird-unread
